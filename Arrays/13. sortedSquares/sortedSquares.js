const sortedSquares = (nums) => {
  let start = 0
  let end = nums.length - 1
  let sortedIndx = end
  let sortedArr = []

  while (sortedIndx < -1) {
    if (Math.abs(nums[start++]) < Math.abs(nums[end--])) {
    }
  }
}
